#include <stdio.h>

void main(){
    int m[] = {5,6,0,4,5,7,5};
    int arrayLength = sizeof(m) / sizeof(m[0]);
        printf("%d number ",arrayLength);

    int sum = 0;
    for(int i=0;i<arrayLength;i++){
        sum+=m[i];
    }
    printf("sum =%d",sum);


    getch();
}
